sudo: required
services:
  - docker

before install:
  - docker build -t SaonMustafi/docker-react -f Dockerfile.dev .

language: node_js

scripts:
  - docker run SaonMustafi/docker-react npm run test -- --coverage